import React from "react";
import styles from "../header/header.module.css";

export default () => (
  <div className={styles.preHeader}>
    <h2>Eveline's Amazing</h2>
    <h1 className={styles.header}>Cat Blog</h1>
  </div>
);
